import { StyleSheet } from "react-native";
import { scale } from "../../../utils/scale";

export default StyleSheet.create({
  container: {
    alignSelf: "flex-end",
    marginTop: scale(8),
  },
});
